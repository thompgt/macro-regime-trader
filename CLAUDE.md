# macro_regime_trader — project instructions

## Commit practice
Commit and push **frequently** — after each individual file or small logical
unit lands, not batched at the end of a phase or bullet in `workplan.md`.
Small, frequent, pushed commits are the default working style for this repo.
Update `workplan.md` checkboxes in the same commit as the work they track.
